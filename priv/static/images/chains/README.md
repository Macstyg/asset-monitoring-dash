These network SVGs are local copies from `@web3icons/core` v4.0.51.

Source package: https://www.npmjs.com/package/@web3icons/core
License: MIT

The app uses the `networks/background` variants so the icons stay legible inside compact circular badges.
